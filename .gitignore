# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# local workspace — listing assets, uploads, generated output, and the SQLite
# database all live here. NEVER commit real client data or the database.
/workspace/
!/workspace/.gitkeep

# env — never commit real keys
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

# testing
/coverage
